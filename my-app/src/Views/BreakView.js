import React, { Component } from 'react';

class BreakView extends Component {
    // Pre-Conditions: The breaks tab is selected in the navigation
    // Post-Conditions: If thers is a break happening, display time 
    // remaining in current break. Otherwise display time until 
    // next brain
    displayRemainingTime() {
    }
    
    render() {
        return (
            <div>
            </div>
        )
    }
}